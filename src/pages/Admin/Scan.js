import React from 'react';
import ScanTables from "../../components/Scan/ScanTables";

const Scan = () => {
    return (        
        <div className="Scan">
            <ScanTables />
        </div>
    );
};

export default Scan;
